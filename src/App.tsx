import { createBrowserRouter, Routes, Route, Outlet } from "react-router-dom";
import "./App.css";

function App() {
  return (
    <div>
      App
      <Outlet />
    </div>
  );
}

export default App;
